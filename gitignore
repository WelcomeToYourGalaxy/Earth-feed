# Local environment
.venv/
__pycache__/
*.pyc

# Full harvest with article bodies — 19 MB, and wire.json carries the same
# items trimmed for the browser. Regenerated by every run.
raw_items.json

# macOS
.DS_Store
